#!/usr/bin/env sh

# concat into complete procfile
touch tmp/Procfile_complete

cat "# This is autogenerated every time the dev cluster is started" > Procfile_complete

cat Procfile >> Procfile_complete

./services/arkd/bin/before_start
cat arkd/Procfile >> Procfile_complete

hivemind Procfile_complete
